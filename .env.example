## Example .env for local development
# Copy to .env and update values as needed. Do NOT commit your real credentials.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sanfrancisco
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# If set to 1, forces use of MySQL and will not silently fall back to SQLite
PREFER_MYSQL=0
